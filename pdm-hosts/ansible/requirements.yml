---
# Galaxy collections the pdm-host role pulls. Install with:
#   ansible-galaxy collection install -r requirements.yml
# Or:
#   just pdm-hosts-deps
collections:
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
