# Managed by Ansible — vms/openbao/ansible/roles/openbao/files/openbao-override.conf
#
# Drops into /etc/systemd/system/openbao.service.d/override.conf to
# re-assert mlock-related settings on top of the apt-installed unit.
# Idempotent if upstream already sets these.

[Service]
LimitMEMLOCK=infinity
AmbientCapabilities=CAP_IPC_LOCK
CapabilityBoundingSet=CAP_IPC_LOCK
