---
# Galaxy collections this role pulls. Install with:
#   ansible-galaxy collection install -r requirements.yml
# (or `just ansible-deps rootca`).
collections:
  - name: community.general
    version: ">=8.0.0"
