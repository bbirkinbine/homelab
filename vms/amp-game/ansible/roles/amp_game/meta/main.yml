---
galaxy_info:
  role_name: amp_game
  author: brian.birkinbine
  description: AMP (CubeCoders Application Management Panel) prerequisites on Ubuntu 24.04 (Packer base). Installs apt deps, opens ufw ports, enables unattended-upgrades. Operator runs getamp.sh post-Ansible.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - noble

dependencies: []

collections:
  # `community.general.ufw` — apt's ufw module ships there. Make the
  # dependency explicit so a fresh Ansible install knows what to fetch.
  - community.general
